using UnityEngine;
using System.Collections;

public class Audio : MonoBehaviour {
	private float anal;
	/*private Buttons btns;

	void Awake(){
		btns = FindObjectOfType<Buttons> ();
	}

	void Start(){
		if(PlayerPrefs.GetInt("Sound") == 1){
			for (int i = 0; i < btns.soundSource.Length; i++) {
				btns.soundSource[i].GetComponent<AudioSource> ().volume = 1;
			}
		}else{
			for (int i = 0; i < btns.soundSource.Length; i++) {
				btns.soundSource [i].GetComponent<AudioSource> ().volume = 0;
			}
		}

		if(PlayerPrefs.GetInt("Music") == 1){
			for (int i = 0; i < btns.soundSource.Length; i++) {
				btns.soundSource[i].GetComponent<AudioSource> ().volume = 1;
			}
		}else{
			for (int i = 0; i < btns.soundSource.Length; i++) {
				btns.soundSource [i].GetComponent<AudioSource> ().volume = 0;
			}
		}
	}*/
}
